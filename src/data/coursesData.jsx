/* =========================================================
   LEGACY RE-EXPORT
   Kept so existing imports keep working.
   New code should import from "../data" instead.
========================================================= */

export { coursesData, availableCourses, courseGroups } from "./index";